DB management:
  ✔ Ini or Yml configuration file @done(18-09-05 22:53)
  ✔ Switch to sqlachemy @done(18-09-05 22:08)
  ✔ Use pandas / sqlalchemy and remove all sql commands @done(18-09-05 22:08)
  ☐ Being to track how many entries changed between two commits
  ☐ Automatic saving of the tables in another DB for incident recoveries
    ☐ For instance table names with dates and keep the last X defined in the configuration file
  ☐ Initialisation: if the table exists, check that the columns are in the right types

Tag Management:
  ✔ Avoir une col tags @done(18-09-05 22:54)
  ☐ Lors du chargement decoder chaque pour avoir une liste complete de tags
  ☐ Creer les cols avec False
  ☐ Puis parser les str avec contains r''
  ☐ retourner liste des tags
  ☐ a chaque nouveau regex on update la column tags bien entendu

Ideas / Principles:
  ☐ Regexes can match one or more cols to define a tag
  ☐ A user can set for a specific line a specific tag
    ☐ Even if this could also be expressed in the regex table, we will put them in another
  ☐ We should be always able to recreate the data by using:
    ☐ The regex table
    ☐ The custom table

